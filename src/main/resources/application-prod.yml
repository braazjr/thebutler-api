server:
  port: ${PORT}
  host: ${HOST}

spring:
  data:
    mongodb:
      database: ${MONGODB_DATABASE}
      uri: ${MONGODB_URI}

thebutler:
  s3:
    profile: prod
    access-key-id: ${S3_ACCESSKEYID}
    secret-access-key: ${S3_SECRETACCESSKEY}
    bucket-photos: thebutler-photos
    bucket-documents: thebutler-documents
    originsPermitidas: http://localhost:4200,http://localhost:3000,https://thebutler-web-dev.herokuapp.com,https://thebutler-web.herokuapp.com,file://,http://localhost,https://thebutler.com.br,https://www.thebutler.com.br