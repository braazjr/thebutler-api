server:
  port: 8081

spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: thebutler-api

thebutler:
  s3:
    profile: local
    access-key-id: ${S3_ACCESSKEYID}
    secret-access-key: ${S3_SECRETACCESSKEY}
    bucket-photos: thebutler-photos-dev
    bucket-documents: thebutler-documents-dev
  originsPermitidas: http://localhost:4200,http://localhost:3000,http://localhost:8100,http://localhost:8101,https://thebutler-web.herokuapp.com,file://